# Auto-generated CI workflow for comprehensive tests
# Do not edit manually - regenerate using npm run generate:ci

name: CI Pipeline - Comprehensive

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

env:
  NODE_VERSION: 18
  CI: true
  NODE_OPTIONS: --max-old-space-size=1024

jobs:
  comprehensive-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright browsers
        run: npx playwright install --with-deps
      - name: Run comprehensive tests
        run: npm run test:integration && npm run test:unit && npm run test:e2e:smoke && npm run typecheck && npm run lint
        env:
